
<h1 className="slide-title">new instance</h1>

<div className="slide-subtitle">
<h2>или рассказ о том как<br/> сделать экземпляр экземпляра</h2>
</div>

<h1 className="borderline">8</h1>

-----

# Зачем self reconstruct ?

<div className='codemedium'>

```javascript

const { HTMLElement } = window;

class MyHTMLElement extends HTMLElement {}

customElements.define('my-custom-element', MyHTMLElement);
const myElement = document.createElement('my-custom-element');

console.log('myElement instanceof MyHTMLElement',
    myElement instanceof MyHTMLElement);    // true
console.log('myElement instanceof HTMLElement',
    myElement instanceof HTMLElement);      // true
```
</div>

-----

# Зачем self reconstruct ?

<div className='codemedium'>

```javascript
class MyHTMLElement extends HTMLElement {
    communicate ( value ) {
        // всё те же игрушки ... 
        this.innerHTML = `${this.protoAddition} + ${this.addition} + ${value}`;
    }
    addition = 'addition';
}
const protoProps = {
    protoAddition: 'protoAddition',
};
Object.setPrototypeOf(protoProps, HTMLElement.prototype);
```
</div>

-----

# Зачем self reconstruct ?

<div className='codemedium'>

```javascript
const proxy = new Proxy(protoProps, {
    get (target, prop, receiver) {
        const result = Reflect.get(target, prop, receiver);
        console.log('get:', prop, result);
        return result;
    },
    set (target, prop, value, receiver) {
        const result = Reflect.set(target, prop, value, receiver);
        console.log('set:', prop, result, value);
        return result;
    } // ↓↓↓ и далее всё, что нужно ...

```
</div>

-----

# Зачем self reconstruct ?

<div className='codemedium'>

```javascript
Object.setPrototypeOf(MyHTMLElement.prototype, proxy);

customElements.define('my-custom-element', MyHTMLElement);
const myElement = document.createElement('my-custom-element');
console.log('myElement instanceof MyHTMLElement',
    myElement instanceof MyHTMLElement);
console.log('myElement instanceof HTMLElement',
    myElement instanceof HTMLElement);

console.log('render begins');   // magic happens here
myElement.innerText = 123;      // set скажет в консоль

```
</div>


<Button onClick={() => { openExample('MyHTMLElement')}}>Run Me</Button>

-----

# Спросил Gemini AI : &laquo; Можешь Повторить &raquo; ?

<div className='codemedium'>

```javascript

For Self Reconstruct: 

The Working Solution: .spawn() Method.
We can perfectly replicate the behaviour
of self-reconstruction and prototype chaining
when using a method instead of the `new operator`.

Here is the corrected code.

It replaces new myElement with myElement.spawn(),
which is the only valid way to achieve this in the DOM.

```
</div>

<Button onClick={() => { openExample('GeminiTest')}}>Run Me</Button>

-----

# Ответ Gemini AI : да &laquo; давай ещё по одной &raquo; ...

<div className='codemedium'>

```javascript
You are most welcome.

It is a very powerful architectural pattern because it effectively
turns your DOM tree into a live inheritance stack—allowing
child elements to "subscribe" to data changes at their parent elements
without any complex event listeners or state management libraries.

For Self Reconstruct: 
 The .spawn() method bridges the gap between the rigid
 HTMLElement C++ layer and the flexible JavaScript prototype system,
 giving you the "self-reconstruction" behavior you were looking.

```
</div>

<Button onClick={() => { openExample('GeminiTest_Re_Re')}}>Run Me</Button>


-----

# Спросил KIMI K2.6: &laquo; умеете, ну &mdash; могёте &raquo;

<div className='codemedium'>

```javascript
Purpose:
  Useful above meta-programming, prototype-chain-based state machines,
  component templating, and demonstrating advanced JS prototype
  composition patterns.

Technique:
  When `new myElement` is called on an existing instance,
  `Self` detects and  fixes the prototype chain within
  `Object.setPrototypeOf(ogp(this), root)`, returning fresh `Self`.
  This creates a chain where `new instance` always produces a clone
  keeping intact identity `instanceof MyHTMLElement` to `constructor.name`.
```
</div>

<Button onClick={() => { openExample('MyHTMLElementReconstructible')}}>Run Me</Button>

-----

# KIMI : Final Architecture

<Box>
<div className='codemedium'>
```javascript
seq (Self function, from new reconstructed)
  → prototype: { constructor: MyHTMLElement }
  → [[ Prototype ]]: reconstructed ( another Self )
    → [[ Prototype ]]: myElement ( another Self )
      → [[ Prototype ]]: proxy
        → [[ Prototype ]]: MyHTMLElement.prototype
          → [[ Prototype ]]: SelfReconstructibleHTMLElement.prototype
            → [[ Prototype ]]: HTMLElement.prototype
              → [[ Prototype ]]: Element.prototype
                → [[ Prototype ]]: Node.prototype
                  → [[ Prototype ]]: EventTarget.prototype
                    → [[ Prototype ]]: Object.prototype
                      → null
```
</div>
</Box>

-----

# Как выглядит паттерн Self Reconstructor в JS

<div className='codemedium'>

```javascript
const SelfReConstructor = function () {
    const main = this.constructor;
    const isItSelf = main === SelfReConstructor;
    const Self = function () {
        if (new.target) {
            return new (isItSelf ?
                SelfReConstructor : main);
        }
        return Self;
    };
    Object.setPrototypeOf(Self, this);
    Self.prototype.constructor = this;
    return Self;
};
```

</div>

-----

# и, да, то есть потом можно вот так

<div className='codemedium'>

```javascript
const item = new SelfReConstructor();

// item — это функция!
item();           // callable ✓
new item();       // newable ✓

// и при этом...
item instanceof SelfReConstructor;  // true
item instanceof Function;           // true
item instanceof Object;             // true
```

</div>


<div className="slide-callout">
<h2 className="danger"><code className="inlineCode">item</code> — одновременно и Тип и Интерфейс !</h2>
</div>

-----

# и ещё вот так ...

<div className='codemedium'>

```javascript
const re = new item;  // new item() работает!

console.log(re instanceof item);                // true ?!
console.log(re instanceof SelfReConstructor);   // true
console.log(re instanceof Function);            // true
console.log(re instanceof Object);              // true

class MyInheritedItem extends re {
    /* yes, it works */
}

```

</div>

<div className="slide-callout">
<h2 className="secondary"><code className="inlineCode">MyInheritedItem instanceof item</code></h2>
</div>

-----


# запустим и посмотрим

<div className='codemedium'>

```javascript
const re = new item;  // new item() работает!

console.log(re instanceof item);                // true ?!
console.log(re instanceof SelfReConstructor);   // true
console.log(re instanceof Function);            // true
console.log(re instanceof Object);              // true

class MyInheritedItem extends re {
    /* yes, it works */
}

```

</div>

<div className="slide-callout">
<h2 className="secondary"><code className="inlineCode">MyInheritedItem instanceof item</code></h2>
</div>

<Button onClick={() => { runExample('self_reconstruct')}}>Run Me</Button>

-----

# А как в TypeScript?

<div className='codemedium'>

```typescript
// self — это и экземпляр, и конструктор
interface Self {
    new (): Self;
    value: number;
}
// Identity pattern: объект знает, как себя пересоздать
interface Reconstructible<T> extends NewableFunction {
    new (...args: any[]): T & Reconstructible<T>;
    prototype: Reconstructible<T>;
}
// TS видит recursive тип
const item: Self = new (Cstr as Reconstructible<Self>);
const re: Self = new item;   // ok — item is constructible
```

</div>

<div className="slide-callout">
<h3 className="secondary">Identity pattern: тип сам себя порождает — рекурсивный <code className="inlineCode">new () =&gt; Self</code></h3>
</div>

-----

# Зачем само-воспроизведение

<Box sx={{marginTop: '7vh', marginLeft: '15vw', fontFamily: 'ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace', fontSize:'3vh'}}>

<h3>• Пересоздать экземпляр с нуля</h3>
<h3>• Склонировать с новыми аргументами</h3>
<h3 className="secondary">• Undo / Redo: повторить создание, откатить изменения</h3>
<br/>
<h3><code className="inlineCode">new myItem</code> — <br/> &nbsp; каждый объект знает, как себя пересоздать</h3>
<h3><code className="inlineCode">new myItem.MyReConstructibleField</code> — <br/> &nbsp; удобно для типизированных полей</h3>

</Box>

-----

# &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; вот теперь ...

<div>
<Box sx={{textAlign: 'center'}}>
<img src="./content/dip.png" style={{width: '67%', marginTop: '5vh', borderRadius: '2vh'}} />
</Box>
</div>