
<h1 className="slide-title">await new</h1>

<div className="slide-subtitle">
<h2>можно ли создать <br/>экземпляр<br/> асинхронно?</h2>
</div>

<h1 className="borderline">7</h1>

-----

# await new — class + result

<div className='codemedium'>

```javascript
class MyPromise extends Promise {
    constructor(handler) { super(handler); }
}

const myItem = new MyPromise((resolve) => { resolve(123); });

// Мы можем await-ить результат new!
console.log(await myItem);  // 123

// И проверить наследование:
console.log(myItem instanceof Promise);  // true
```

</div>

<div className="slide-callout">
<h3><code className="inlineCode">new</code> + <code className="inlineCode">await</code> === новая семантика!</h3>
</div>

-----

# запустим и посмотрим

<div className='codemedium'>

```javascript
class MyPromise extends Promise {
    constructor(handler) { super(handler); }
}

// new возвращает thenable
// await автоматически разворачивает его
console.log(await new MyPromise((resolve) => {
    resolve(321);
}));  // 321

// Граница между sync и async construction размыта
```

</div>

<Button onClick={() => { runExample('await_new_problems')}}>Run Me</Button>

<div className="slide-callout">
<h2 className="secondary">и да: и <code className="inlineCode">new</code> и <code className="inlineCode">await</code> — это Интерфейс</h2>
</div>

-----

# можно и без наследования от Promise

<div className='codemedium'>

```javascript
class MyAsyncClass {
    field = undefined;
    constructor() {
        const self = this;
		return new Promise((resolve) => {
            setTimeout(() => {
                self.field = 123;
                resolve(self);
            }, 1000);
        });
    }
}
```

</div>

-----

# можно и без наследования от Promise

<div className='codemedium'>

```javascript
class MySubAsyncClass extends MyAsyncClass {
    constructor() {
        // да, super возвращает результат ! 
        const promise = super();
        return new Promise(async (resolve) => {
            // тут item содержит  this !!!
            const item = await promise;
            console.log(item.field); // 123
            resolve(item);
        });
    }
}
```

</div>


-----

# запустим и посмотрим

<div className='codemedium'>

```javascript

    ( async () => {

        // создадим ожидание наследника
        const successor = new MySubAsyncClass;

        // создадим сам экземпляр наследника
        const item = await successor;

        // проверим, что всё получилось 
        console.log(item.field); // 123

} )();

```

</div>

<Button onClick={() => { runExample('asyncClassTest')}}>Run Me</Button>

-----

# А как в TypeScript?

<div className='codemedium'>

```typescript
interface AsyncConstructor<T> extends NewableFunction {
    new (...args: any[]): Promise<T>;
}
class MyPromise<T> extends Promise<T> {
    constructor(executor: (
      resolve: (value: T) => void, reject: (reason?: any) => void
    ) => void) {
        super(executor);
    }
}
// await new — тип выводится как T, не Promise<T>
const item: number = await new MyPromise(
    (resolve) => resolve(123)
);
```

</div>

<div className="slide-callout">
<h3 className="secondary">constructor возвращает Promise&lt;T&gt; — await разворачивает до T</h3>
</div>

-----

# Паттерн: <code className="inlineCode">await new</code>
<Box sx={{marginTop: '7vh', marginLeft: '15vw', fontFamily: 'ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace', fontSize:'3vh'}}>

<div style={{display: 'grid', gridTemplateColumns: '1.0fr 1.0fr'}}>

<div>
<h3>Вместо: <br/><code className="inlineCode">new Client()</code>+ <br/><code className="inlineCode">await client.init()</code><br/> 
конструктор сразу <br/>возвращает Promise</h3>
<h3 className="secondary">То есть единый : <br/> <code className="inlineCode">await new Client()</code><br/> 
вместо &laquo; лапши &raquo; из двух строк</h3>
</div>

<div>
<br/>
</div>

</div>
</Box>

-----

# Паттерн: <code className="inlineCode">await new</code>

<Box sx={{marginTop: '7vh', marginLeft: '15vw', fontFamily: 'ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace', fontSize:'3vh'}}>

<div style={{display: 'grid', gridTemplateColumns: '1.0fr 1.0fr', gap: '3vw'}}>

<div>
<h3>Вместо: <br/><code className="inlineCode">new Client()</code>+ <br/><code className="inlineCode">await client.init()</code><br/>
конструктор сразу <br/>возвращает Promise</h3>
<h3 className="secondary">То есть единый : <br/> <code className="inlineCode">await new Client()</code><br/> 
вместо &laquo; лапши &raquo; из двух строк<br/>&laquo;скованных одной цепью&raquo;
<br/> &laquo;связанных одной целью&raquo;</h3>
</div>

<div>
<img src="./content/nau.webp" style={{width: '29vw', marginTop: '5vh', borderRadius: '2vh'}} />
</div>

</div>
</Box>


-----

# &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; пу пу пу ...

<div>
<Box sx={{textAlign: 'center'}}>
<img src="./content/dip.png" style={{width: '67%', marginTop: '5vh', borderRadius: '2vh'}} />
</Box>
</div>
