
<h1 className="slide-title">const не так прост</h1>

<div className="slide-subtitle">
<h2>или почему <code className="inlineCode">const &#123; prop &#125; = obj</code><br/> делает не то, что вы думаете</h2>
</div>

<h1 className="borderline">3</h1>

-----

# const &mdash; это про привязку, не про значение

<div className='codemedium'>

```javascript
let toBeProp = 5;
const myObject = {};
Object.defineProperty(myObject, 'prop', {
    get () {
        return { [Symbol.toPrimitive] () {
            // ← returns CURRENT toBeProp
            return toBeProp; 
        }};
    }
});
const { prop } = myObject;  // prop — НЕ число!
```

</div>

<div className="slide-callout">
<h3>const хранит ССЫЛКУ, не значение. Getter — это Interface.</h3>
</div>

-----

# const &mdash; это про привязку, не про значение

<div className='codemedium'>

```javascript
let toBeProp = 5;
const myObject = {};
Object.defineProperty(myObject, 'prop', {
    get () {
        return { [Symbol.toPrimitive] () {
            // ← returns CURRENT toBeProp
            return toBeProp; 
        }};
    }
});
const { prop } = myObject;  // prop — НЕ число!
```

</div>

<div className="slide-callout">
<h3 className="secondary">Getter + Symbol.toPrimitive === ленивое вычисление</h3>
</div>

-----

# память указателя на const изменяема !

<div className='codesmall'>

```javascript
let toBeProp = 5;
const myObject = {};

Object.defineProperty(myObject, 'prop', {
    get () {
        return { [Symbol.toPrimitive] () {
            // ← returns CURRENT toBeProp
            return toBeProp; 
        }};
    }
});

const { prop } = myObject;

// Сюрприз Сюрприз : "const" переменная изменила своё значение !:

console.log(prop + 0);   // 5

toBeProp = 7;             // изменили источник

console.log(prop + 0);   // 7 ?!?!?!?
```

</div>

<Button onClick={() => { runExample('const_destructuring')}}>Запустить пример</Button>

-----

# А как в TypeScript?

<div className='codemedium'>

```typescript
// Объект с живым getter'ом
interface LiveNumber {
    [Symbol.toPrimitive](hint: string): number;
}

interface MyObject {
    get prop(): LiveNumber;
}

// Деструктуризация — тип НЕ number!
const { prop } = myObject;  // type: LiveNumber

// Приведение происходит только в контексте
const sum: number = prop + 0;  // ok — toPrimitive сработает
```

</div>

<div className="slide-callout">
<h3 className="secondary">getter + Symbol.toPrimitive = тип, который ведёт себя как number</h3>
</div>

-----

# production

<Box sx={{marginTop: '7vh', marginLeft: '15vw', fontFamily: 'ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace', fontSize:'3vh'}}>

<h3>В Момент, когда const <br/>вдруг меняет значение — вы понимаете:<br/> изменился не const, 
<br/>а тот контекст <code className="inlineCode">[[ scope ]]</code>,<br/> где значение было создано</h3>

<h3 className="secondary"><code className="inlineCode">getter</code> и деструктуризация
<br/> дают живые данные
<br/> вы читаете обновления, <br/>а не храните состояние &laquo;слепка&raquo;</h3>

</Box>

-----
