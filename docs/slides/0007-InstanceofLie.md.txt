<h1 className="slide-title">instanceof <br/> тоже Interface</h1>

<div className="slide-subtitle">
<h2>оператор, <br/>который можно обмануть</h2>
</div>

<h1 className="borderline">4</h1>

-----

# Как работает instanceof?

<div className='codemedium'>

```javascript
function foo() { };
const bar = { a: 'a' };

// Создаём фейковую цепочку прототипов:
Object.setPrototypeOf(foo.prototype, bar);
const baz = Object.create(foo.prototype);

// Проверим:
console.log(baz instanceof foo);  // true
// Но baz НИКОГДА не создавался через new foo()!
```

</div>

<div className="slide-callout">
<h3>Цепочка: baz → foo.prototype → bar</h3>
</div>

-----

# Запустим и посмотрим

<div className='codemedium'>

```javascript
// Та же цепочка:
function foo() { }; 
const bar = { a: 'a' };
Object.setPrototypeOf(foo.prototype, bar);

const baz = Object.create(foo.prototype);

// Ложь:
console.log(baz instanceof foo);  // true
// baz создан от bar, где foo.prototype.__proto__ === bar
```

</div>

<Button onClick={() => { runExample('instanceof_lie')}}>Запустить пример</Button>

<div className="slide-callout">
<h3>baz instanceof foo === true</h3>
</div>

-----

# Запустим и посмотрим

<div className='codemedium'>

```javascript
// Та же цепочка:
function foo() { };
const bar = { a: 'a' };
Object.setPrototypeOf(foo.prototype, bar);

const baz = Object.create(foo.prototype);

// Ложь:
console.log(baz instanceof foo);  // true
// baz создан от bar, где foo.prototype.__proto__ === bar
```

</div>

<Button onClick={() => { runExample('instanceof_lie')}}>Запустить пример</Button>

<div className="slide-callout">
<h2 className="danger">даже если <code className="inlineCode">baz</code> не создавался через <code className="inlineCode">new foo()</code></h2>
</div>

-----

# А как в TypeScript?

<div className='codemedium'>

```typescript
// TS не знает про setPrototypeOf манипуляции
declare const baz: object;

// Правильный подход — branded types
type FooInstance = {
    __brand: 'FooInstance';
    a: string;
};

interface FooConstructor extends NewableFunction {
    new(): FooInstance;
    prototype: FooInstance;
    [Symbol.hasInstance](instance: unknown): boolean;
}

// instanceof можно типизировать
function isFoo(x: unknown): x is FooInstance {
    return x instanceof (FooCtor as any);
}
```

</div>

<div className="slide-callout">
<h3 className="secondary"><code className="inlineCode">Symbol.hasInstance</code> — типизируемая точка входа для проверок</h3>
</div>

-----

# факты: instanceof не проверяет тип

<div className='codemedium'>

```javascript
// instanceof НЕ спрашивает:
"а ты чьих будешь то, уважаемый foo?"

// instanceof отвечает на вопрос: 
есть ли в у foo "крыша" в цепочке прототипов 

//  любезно предоставленная конструктором foo?"
велкаме to the MITM-attack

```

</div>
<div className="slide-callout">
<h2 className="secondary">видимо это был самый быстрый способ</h2>
<h3 className="primary">проверить хоть что-то в 1995 году</h3>
</div>

-----

# итого: instanceof &mdash; это traversal algorithm

```mermaid
graph LR
    A[baz] -->|__proto__| B[foo.prototype]
    B -->|__proto__| C[bar]
    C -->|__proto__| D[Object.prototype]
    D -->|__proto__| E[null]

    B -.->|проверка: .constructor === foo ?| F{совпало!}
    F -->|true| G[instanceof foo === true]
```

<div className="slide-callout">
<h3 className="secondary">instanceof идёт по цепочке <code className="inlineCode">__proto__</code> и ищет совпадение <code className="inlineCode">.constructor</code></h3>
<h3 className="secondary">если находит — возвращает <code className="inlineCode">true</code>,<br/> даже если объект создавался не через <code className="inlineCode">new</code></h3>
<h3 className="primary">и таким образом <code className="inlineCode">instanceof</code> проверяет не факт создания,<br/> а лишь структуру цепочки прототипов</h3>
</div>

-----

# но instanceof можно &laquo; лечить &raquo;

<Box style={{margin: '3%', paddingLeft: '11%'}}>
<h2 className="secondary">потому, что это в первую очередь Алгоритм</h2>
<h3 className="primary">и лишь уже потом &laquo;какой конкретно&raquo;</h3>
<h2 className="secondary">и так как <code className="inlineCode">instanceof</code> — это Интерфейс</h2>
<h3 className="primary">то этот <code className="inlineCode">interface</code> можно переопределить</h3>
<h3 className="primary">так как у него настраиваемая &laquo;точка входа&raquo;:</h3>
</Box>


-----

# но instanceof можно &laquo; лечить &raquo;

<Box style={{margin: '3%', paddingLeft: '11%'}}>
<h2 className="secondary">потому, что это в первую очередь Алгоритм</h2>
<h3 className="primary">и лишь уже потом &laquo;какой конкретно&raquo;</h3>
<h2 className="secondary">и так как <code className="inlineCode">instanceof</code> — это Интерфейс</h2>
<h3 className="primary">то этот <code className="inlineCode">interface</code> можно переопределить</h3>
<h3 className="primary">так как у него настраиваемая &laquo;точка входа&raquo;:</h3>
</Box>

<div className='codebig'>

```javascript
    MySecureConstructor[ Symbol.hasInstance ]
```
</div>

-----

# переопределяем instanceof

<div className='codemedium'>

```javascript
// пример создания для функционального конструктора
const MyFunctionCtor = function () {
    // мы в конструкторе, поэтому тут мы
    /* ... оставляем закладку ... */
    /* ... известную только нам  ... */
}

MyFunctionCtr [ Symbol.hasInstance ] = ( instance ) => {
    // мы в проверке instanceof, поэтому
    /* ... проверяем закладку ... */
    /* ... известную только нам  ... */
}

```

</div>

-----


# переопределяем instanceof

<div className='codemedium'>

```javascript

class MyClassCtor { // и для классового
    constructor [ Symbol.hasInstance ] ( instance ) {
        // мы в конструкторе, поэтому тут мы
        /* ... оставляем закладку ... */
        /* ... известную только нам  ... */
    }
}
MyClassCtor [ Symbol.hasInstance ] = ( instance ) => {
    // мы в проверке instanceof, поэтому
    /* ... проверяем закладку ... */
    /* ... известную только нам  ... */
}

```

</div>

-----

# Зачем задавать Symbol.hasInstance

<Box sx={{marginTop: '7vh', marginLeft: '15vw', fontFamily: 'ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace'}}>

<h3>
<code className="inlineCode">instanceof</code> проверяет только то, что <br/>
<code className="inlineCode">constructor.prototype.constructor</code> совпадает,
<br/> &nbsp; но не сам факт создания через оператор <code className="inlineCode">new</code>
</h3>
<br/>
<h3 className="secondary">
<code className="inlineCode">Symbol.hasInstance</code> <br/>
&nbsp; позволяет проверить: <br/>
&nbsp; а было ли что-то добавлено <br/>
&nbsp; именно во время конструирования ?
<br/>
</h3>

</Box>

-----

# Зачем задавать Symbol.hasInstance

<Box sx={{marginTop: '7vh', marginLeft: '15vw', fontFamily: 'ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace'}}>

<h3>
<code className="inlineCode">instanceof</code> проверяет только то, что <br/>
<code className="inlineCode">constructor.prototype.constructor</code> совпадает,
<br/> &nbsp; но не сам факт создания через оператор <code className="inlineCode">new</code>
</h3>
<br/>
<h3 className="secondary">
<code className="inlineCode">Symbol.hasInstance</code> <br/>
&nbsp; позволяет проверить: <br/>
&nbsp; а было ли что-то добавлено <br/>
&nbsp; именно во время конструирования ?
<br/>&nbsp; известное лишь лисе Алисе и коту Базилио
<br/>
</h3>

</Box>

-----

# Зачем задавать Symbol.hasInstance

<Box sx={{marginTop: '7vh', marginLeft: '15vw', fontFamily: 'ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace'}}>

<h3>
<code className="inlineCode">instanceof</code> проверяет только то, что <br/>
<code className="inlineCode">constructor.prototype.constructor</code> совпадает,
<br/> &nbsp; но не сам факт создания через оператор <code className="inlineCode">new</code>
</h3>
<br/>
<h3 className="secondary">
<code className="inlineCode">Symbol.hasInstance</code> <br/>
&nbsp; позволяет проверить: <br/>
&nbsp; а было ли что-то добавлено <br/>
&nbsp; именно во время конструирования ?
<br/>&nbsp; известное лишь лисе Алисе и коту Базилио
<br/>&nbsp; ну и, конечно же и тебе, мой &laquo;Юный Падаван&raquo;
</h3>

</Box>

-----
